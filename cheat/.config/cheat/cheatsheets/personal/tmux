# To start tmux:
tmux

# To detach from tmux:
Ctrl-Space d

# To restore tmux session:
tmux attach

# To detach an already attached session (great if you are moving devices with different screen resolutions):
tmux attach -d 

# To display session:
tmux ls

# To rename session:
Ctrl-Space $

# To switch session:
Ctrl-Space s

# To start a shared session:
tmux -S /tmp/your_shared_session
chmod 777 /tmp/your_shared_session

# To help screen (Q to quit):
Ctrl-Space ?

# To scroll in window:
Ctrl-Space PageUp/PageDown

# To reload configuration file
Ctrl-Space : source-file /path/to/file

# To create a window:
Ctrl-Space c

# To go next window:
Ctrl-Space n

# To destroy a window:
Ctrl-Space x

# To switch between windows:
Ctrl-Space [0-9]
Ctrl-Space Arrows

# To split windows horizontally:
Ctrl-Space %

# To split windows vertically:
Ctrl-Space "

# To swap windows:
Ctrl-Space :swap-window -s [0-9] -t [0-9]

## My custom plugins


# To open a quick-fix list of all URLs visible in the current pane
Ctrl-Space u

# quickly select, copy/insert/complete text without a mouse
Ctrl-Space Tab
